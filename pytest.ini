[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
markers =
    live: hits a real LLM provider; skipped in CI (Layer-2 evals only)
asyncio_mode = auto
filterwarnings =
    error::DeprecationWarning:app.*
